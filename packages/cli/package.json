{
  "name": "rewter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "rewter": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@rewter/shared": "workspace:*",
    "@rewter/server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
