#!/usr/bin/env node
// rewter CLI — start|stop|status|logs|sync-models|card <model>|install-service|gc
// Command implementations land in M8 (daemonization) and M4 (sync-models/card).
console.log("rewter 0.1.0 — commands land in M4/M8");
